[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  VITE_API_BASE = "https://novapay-api-gateway.onrender.com"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
